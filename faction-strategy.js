import {campaign,saveCampaign} from './campaign-core.js';
import {FACTIONS,SYSTEM_POWERS} from './factions-system.js?v=77';

const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const BASE={central:{power:100,wealth:100,military:100},haven:{power:46,wealth:52,military:43},pelagosA:{power:44,wealth:55,military:46},pelagosB:{power:39,wealth:66,military:31},kestrel:{power:25,wealth:28,military:22},nadir:{power:18,wealth:20,military:19},meridian:{power:42,wealth:78,military:21},orpheus:{power:35,wealth:61,military:27},frontierGuard:{power:24,wealth:17,military:34},redKnives:{power:22,wealth:23,military:37},blackWake:{power:19,wealth:29,military:24}};
const CLASS_RANK={Frigate:1,Destroyer:2,Cruiser:3,Battleship:4,Carrier:4};
function basePresence(system,id){return(SYSTEM_POWERS[system]||[]).find(x=>x.id===id)?.presence||0}
function ensurePresence(){campaign.factionPresence??={};for(const [system,entries] of Object.entries(SYSTEM_POWERS)){campaign.factionPresence[system]??={};for(const e of entries)if(!Number.isFinite(campaign.factionPresence[system][e.id]))campaign.factionPresence[system][e.id]=e.presence}}
function postureFor(state,id){const base=BASE[id]||{military:20},ratio=(state.military||0)/Math.max(1,base.military),p=state.pressure||0;if((state.military||0)<8||ratio<.28||p>78)return'critical';if(ratio<.50||p>58)return'defensive';if(ratio<.72||p>38)return'strained';return'normal'}
function strategicLabel(p){return p==='critical'?'core defence only':p==='defensive'?'concentrating forces':p==='strained'?'reduced projection':'normal operations'}
function activeFrontsFor(id,system=null){return(campaign.strategicFronts?.fronts||[]).filter(f=>f.status==='active'&&(f.a===id||f.b===id)&&(!system||f.system===system))}
function hostilePressure(id){const fronts=activeFrontsFor(id);if(!fronts.length)return 0;let total=0;for(const f of fronts){const control=f.a===id?-(f.control||0):(f.control||0),ex=f.exhaustion?.[id]||0;total+=18+Math.max(0,control)*1.15+ex*.22}return clamp(total/fronts.length,0,100)}
function systemThreat(id,system){const fronts=activeFrontsFor(id,system);if(!fronts.length)return 0;let x=0;for(const f of fronts){const control=f.a===id?-(f.control||0):(f.control||0);x+=.35+Math.max(0,control)/55}return clamp(x,0,1.5)}
function logPostureChange(id,oldPosture,newPosture){if(oldPosture===newPosture)return;campaign.log??=[];const name=FACTIONS[id]?.short||id;if(newPosture==='critical')campaign.log.push(`Day ${campaign.day}: ${name} has pulled back to core defence after sustained pressure.`);else if(newPosture==='defensive')campaign.log.push(`Day ${campaign.day}: ${name} is concentrating forces and reducing peripheral commitments.`);else if(newPosture==='strained')campaign.log.push(`Day ${campaign.day}: ${name} is under pressure; long-range deployments are being cut back.`);else if(oldPosture&&newPosture==='normal')campaign.log.push(`Day ${campaign.day}: ${name} has recovered enough strength to resume normal operations.`);campaign.log=campaign.log.slice(-50)}
export function ensureStrategicFactionState(){campaign.factions??={};ensurePresence();let dirty=false;for(const id of Object.keys(FACTIONS)){const b=BASE[id]||{power:20,wealth:20,military:20},s=campaign.factions[id]??=({...b,status:'active'});if(!Number.isFinite(s.pressure)){s.pressure=0;dirty=true}if(!Number.isFinite(s.readiness)){s.readiness=65;dirty=true}if(!s.posture){s.posture='normal';dirty=true}if(!Number.isFinite(s.lastStrategicDay)){s.lastStrategicDay=campaign.day||1;dirty=true}}campaign.factionStrategicDay??=campaign.day||1;if(dirty)saveCampaign(campaign);return campaign.factions}
function stepOneDay(){ensureStrategicFactionState();for(const [id,s] of Object.entries(campaign.factions)){const pressure=hostilePressure(id),oldPosture=s.posture||'normal';s.pressure=clamp(s.pressure*.82+pressure*.18,0,100);const footprint=Object.values(campaign.factionPresence).reduce((n,m)=>n+(m[id]||0),0),commercial=id==='meridian'?1.45:id==='orpheus'?1.25:1,income=(.08+.045*footprint)*commercial,upkeep=.0012*(s.military||0)+.0007*(s.readiness||0);s.wealth=clamp((s.wealth||0)+income-upkeep-s.pressure*.0015,0,100);const lowReserve=Math.max(0,18-(s.military||0))*.018,recovery=Math.max(0,(s.wealth-18)*.0024+(s.power||0)*.0010+lowReserve),attrition=Math.max(0,s.pressure-34)*.0034;s.military=clamp((s.military||0)+recovery-attrition,6,100);s.readiness=clamp((s.readiness||0)+(.13+(s.wealth||0)*.0022)-s.pressure*.0030+(s.readiness<30?.08:0),10,100);s.power=clamp((s.power||0)*.994+((s.military||0)*.56+(s.wealth||0)*.44)*.006,8,100);s.posture=postureFor(s,id);logPostureChange(id,oldPosture,s.posture);s.status=(s.military<10&&s.power<12)?'reduced':'active';s.lastStrategicDay=campaign.day;const projection=s.posture==='critical'?.20:s.posture==='defensive'?.42:s.posture==='strained'?.70:1;for(const system of Object.keys(SYSTEM_POWERS)){campaign.factionPresence[system]??={};const base=basePresence(system,id),home=FACTIONS[id]?.home===system,cur=campaign.factionPresence[system][id]||0,threat=systemThreat(id,system);let target=base*(home?clamp(.75+(s.power||0)/180,.75,1.15):projection*clamp(.55+(s.power||0)/90,.35,1.12));if(!home)target*=clamp(1-threat*.20,.55,1);else target=Math.max(target,base*.60);const rate=home?.028:.018;let next=cur+(target-cur)*rate;if(!home&&s.posture==='critical')next-=.0025;if(!home&&s.posture==='defensive')next-=.0010;campaign.factionPresence[system][id]=clamp(next,home?Math.max(.03,base*.45):0,1.15)}}campaign.factionStrategicDay=campaign.day}
export function advanceFactionStrategy(targetDay=campaign.day){ensureStrategicFactionState();let d=campaign.factionStrategicDay||targetDay;const end=Math.max(d,Math.floor(targetDay||d));let steps=0;while(d<end&&steps<365){d++;campaign.factionStrategicDay=d;stepOneDay();steps++}if(steps)saveCampaign(campaign);return steps}
export function strategicPresence(id,system){ensureStrategicFactionState();return campaign.factionPresence?.[system]?.[id]??basePresence(system,id)}
export function factionStrategicSummary(id,system=campaign.location){ensureStrategicFactionState();advanceFactionStrategy(campaign.day);const s=campaign.factions?.[id];if(!s)return null;const p=strategicPresence(id,system);return{id,posture:s.posture||'normal',postureLabel:strategicLabel(s.posture||'normal'),pressure:Math.round(s.pressure||0),readiness:Math.round(s.readiness||0),military:Math.round(s.military||0),wealth:Math.round(s.wealth||0),power:Math.round(s.power||0),presence:p,home:FACTIONS[id]?.home===system,status:s.status||'active'}}
export function factionForcePlan(id,system=campaign.location,requested=1,purpose='general'){const x=factionStrategicSummary(id,system);if(!x)return{count:1,maxClass:1,posture:'normal',presence:0,reason:'unknown force'};const core=x.home||x.presence>=.58,defence=purpose==='defence'||purpose==='antiPiracy';let count=Math.max(0,Math.round(requested));if(x.presence<.035&&!core)count=0;else if(x.posture==='critical')count=core?Math.min(2,Math.max(1,count)):Math.min(1,count);else if(x.posture==='defensive')count=core?Math.min(3,Math.max(1,count)):Math.min(1,count);else if(x.posture==='strained')count=Math.min(2,Math.max(1,count));else count=Math.min(3,Math.max(1,count));if(x.readiness<25)count=Math.min(count,1);else if(x.readiness<45)count=Math.min(count,2);if(defence&&core&&x.posture!=='critical'&&x.readiness>=45)count=Math.min(3,Math.max(count,2));let maxClass=x.military>=62?4:x.military>=34?3:x.military>=18?2:1;if(x.readiness<32)maxClass=Math.min(maxClass,2);if(!core&&x.posture==='defensive')maxClass=Math.min(maxClass,2);if(x.posture==='critical')maxClass=1;return{...x,count,maxClass,core,purpose,reason:core?`forces concentrated around ${system}`:`${x.postureLabel} at ${system}`}}
export function commitFactionForce(id,plan){ensureStrategicFactionState();const s=campaign.factions?.[id];if(!s||!plan)return null;const cost=Math.max(0,plan.count||0)*(plan.core?2.2:4.8)*(1+(plan.maxClass||1)*.18);s.readiness=clamp((s.readiness||0)-cost,10,100);s.wealth=clamp((s.wealth||0)-Math.max(0,plan.count||0)*.22,0,100);s.posture=postureFor(s,id);saveCampaign(campaign);return{readinessCost:cost,readiness:s.readiness}}
export function hullAllowedByPlan(hull,plan){return(CLASS_RANK[hull?.shipClass]||1)<=Math.max(1,plan?.maxClass||1)}
export function applyFactionLosses(id,{military=0,power=0,wealth=0,presenceSystem=campaign.location,presence=0}={}){ensureStrategicFactionState();const s=campaign.factions?.[id];if(!s)return null;const old=s.posture||'normal';s.military=clamp((s.military||0)-military,6,100);s.power=clamp((s.power||0)-power,8,100);s.wealth=clamp((s.wealth||0)-wealth,0,100);s.readiness=clamp((s.readiness||0)-military*.7,10,100);s.pressure=clamp((s.pressure||0)+military*.7,0,100);s.posture=postureFor(s,id);s.status=(s.military<10&&s.power<12)?'reduced':'active';logPostureChange(id,old,s.posture);if(presenceSystem&&campaign.factionPresence?.[presenceSystem]){const home=FACTIONS[id]?.home===presenceSystem,base=basePresence(presenceSystem,id),floor=home?Math.max(.03,base*.45):0;campaign.factionPresence[presenceSystem][id]=clamp((campaign.factionPresence[presenceSystem][id]||0)-presence,floor,1.15)}saveCampaign(campaign);return s}
ensureStrategicFactionState();advanceFactionStrategy(campaign.day);
if(typeof window!=='undefined'){window.__factionStrategy={advanceFactionStrategy,factionStrategicSummary,factionForcePlan,commitFactionForce,applyFactionLosses};setInterval(()=>advanceFactionStrategy(campaign.day),1200)}