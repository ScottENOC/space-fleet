import {campaign} from './campaign-core.js';
import {vaultSummary,openVault,inspectVaultRecord,requestSealedAppendix,reconstructDeniedArchive,respectArchiveBoundary} from './lacuna-vault.js?v=86';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let sig='';
function host(){const base=document.querySelector('#lacunaConcordPanel')||document.querySelector('#lacunaInnerPanel')||document.querySelector('#lacunaActivityPanel');if(!base)return null;let h=document.querySelector('#lacunaVaultPanel');if(!h){h=document.createElement('section');h.id='lacunaVaultPanel';h.className='investigationPanel mainPlotPanel';base.after(h)}return h}
function recordCard(r){return `<article class="lead ${r.available&&!r.resolved?'available':''}"><span>${esc(r.name)}</span><p>${esc(r.text)}</p><small>${r.resolved?'RECONSTRUCTED':`${r.cost} supplies · ${r.days} day${r.days===1?'':'s'}`}</small>${r.available&&!r.resolved?`<button data-vault-record="${r.id}">Reconstruct record</button>`:''}</article>`}
function render(){const h=host();if(!h)return;const s=vaultSummary();if(!s.available){h.hidden=true;return}h.hidden=false;const signature=JSON.stringify([campaign.day,campaign.supplies,s]);if(signature===sig)return;sig=signature;
 if(!s.opened){h.innerHTML=`<div class="mysteryHead"><div><small>FOUNDING VAULT</small><h3>Reconstruct the closure record</h3></div><strong>${esc(String(s.concordAccess).toUpperCase())}</strong></div><p class="mysterySummary">The political settlement determines how much of Concord’s closure-era archive you can see directly. Even friendly access still requires time, supplies and careful reconstruction.</p><button data-vault-open>Open closure archive · 1 day</button>`;h.querySelector('[data-vault-open]').onclick=()=>{const r=openVault();if(!r.ok)alert(r.reason||'Unable to open archive.');sig='';render()};return}
 if(s.complete){h.innerHTML=`<div class="mysteryHead"><div><small>FOUNDING VAULT</small><h3>${s.nextLead?'Closure reconstructed':'Archive boundary accepted'}</h3></div><strong>${s.nextLead?'ORISON':'CASE LIMITED'}</strong></div><p class="mysterySummary">${s.nextLead?'The Burn and deliberate route closure are now substantially reconstructed. One complete copy of the technical archive escaped Lacuna aboard a courier bound for a continuity site called ORISON. That courier never returned.':'You chose not to breach Concord’s sealed routing appendix. The Lacuna case can remain politically settled without knowing where the final route archive went.'}</p>${(s.history||[]).length?`<div class="mysteryBlock"><b>ARCHIVE LOG</b><ul>${s.history.map(x=>`<li>Day ${x.day}: ${esc(x.text)}</li>`).join('')}</ul></div>`:''}<article class="mysteryDirective"><b>NEXT PHASE</b><p>${s.nextLead?'Determine where ORISON is, whether the courier reached it, and why no return signal was ever recorded.':'No deeper route lead is available unless the archive boundary is revisited later.'}</p></article>`;return}
 const accessText=s.access==='full'?'Concord archivists are supervising direct access to original media.':s.access==='restricted'?'You have selected extracts only. The sealed routing ledger remains outside the protocol.':s.access==='remote'?'You are reconstructing the archive without Concord cooperation; provenance is weaker and costs are higher.':'Concord has denied archive access.';
 h.innerHTML=`<div class="mysteryHead"><div><small>FOUNDING VAULT</small><h3>Closure-era archive</h3></div><strong>${esc(String(s.access).toUpperCase())}</strong></div><p class="mysterySummary">${esc(accessText)}</p><div class="mysteryBlock"><b>ARCHIVE RECORDS</b>${s.records.map(recordCard).join('')}</div>${s.canRequestAppendix?`<div class="mysteryBlock"><b>SEALED APPENDIX</b><p>Under Limited Contact, the routing ledger is withheld. You can ask Concord to revisit the decision; success depends on current trust.</p><button data-vault-appendix>Request sealed appendix review · 2 days</button><button data-vault-respect>Respect archive boundary</button></div>`:''}${s.canRemote?`<div class="mysteryBlock"><b>NO ACCESS</b><p>Concord will not open the Vault. The fleet can attempt a costly remote reconstruction from captured telemetry and surviving station buffers instead.</p><button data-vault-remote>Remote reconstruction · 18 supplies · 3 days</button></div>`:''}${(s.history||[]).length?`<div class="mysteryBlock"><b>ARCHIVE LOG</b><ul>${s.history.map(x=>`<li>Day ${x.day}: ${esc(x.text)}</li>`).join('')}</ul></div>`:''}`;
 h.querySelectorAll('[data-vault-record]').forEach(b=>b.onclick=()=>{const r=inspectVaultRecord(b.dataset.vaultRecord);if(!r.ok)alert(r.reason||'Unable to reconstruct record.');sig='';render()});const app=h.querySelector('[data-vault-appendix]');if(app)app.onclick=()=>{const r=requestSealedAppendix();if(!r.ok)alert(r.reason||'Appendix request failed.');else if(!r.granted)alert('Concord refused the sealed appendix under the current trust level.');sig='';render()};const remote=h.querySelector('[data-vault-remote]');if(remote)remote.onclick=()=>{if(!confirm('Proceed with remote archive reconstruction without Concord cooperation?'))return;const r=reconstructDeniedArchive();if(!r.ok)alert(r.reason||'Remote reconstruction failed.');sig='';render()};const respect=h.querySelector('[data-vault-respect]');if(respect)respect.onclick=()=>{if(!confirm('Respect the archive boundary and close the Lacuna investigation without the routing ledger?'))return;const r=respectArchiveBoundary();if(!r.ok)alert(r.reason);sig='';render()}}
render();setInterval(render,500);
