import {campaign} from './campaign-core.js';
import {peregrineCouncilSummary,openCouncilMeeting,acceptCouncilInspection,askCouncilTopic,chooseReturnRoutePolicy} from './peregrine-council.js?v=91';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let sig='';
function host(){const base=document.querySelector('#peregrineContactPanel')||document.querySelector('#peregrinePanel');if(!base)return null;let h=document.querySelector('#peregrineCouncilPanel');if(!h){h=document.createElement('section');h.id='peregrineCouncilPanel';h.className='investigationPanel mainPlotPanel';base.after(h)}return h}
function logHtml(s){return(s.history||[]).length?`<div class="mysteryBlock"><b>COUNCIL RECORD</b><ul>${s.history.map(x=>`<li>Day ${x.day}: ${esc(x.text)}</li>`).join('')}</ul></div>`:''}
function topicCard(t){return `<article class="lead ${t.available?'available':''}"><span>${esc(t.name)}</span><p>${esc(t.summary)}</p><small>${t.resolved?'DISCUSSED':'1 day'}</small>${t.available?`<button data-council-topic="${esc(t.id)}">Ask the Council</button>`:''}</article>`}
function render(){const h=host();if(!h)return;const s=peregrineCouncilSummary();if(!s.available){h.hidden=true;return}h.hidden=false;const signature=JSON.stringify([campaign.day,s]);if(signature===sig)return;sig=signature;
 if(!s.meetingOpened){h.innerHTML=`<div class="mysteryHead"><div><small>PEREGRINE</small><h3>The Continuity Council</h3></div><strong>TRUST ${s.trust}</strong></div><p class="mysterySummary">PEREGRINE has acknowledged the fleet, but recognition is not the same thing as access. The Council controls the surviving continuity archives and whatever remains of the founders’ route geometry.</p><button data-council-open>Request formal Council meeting · 1 day</button>`;h.querySelector('[data-council-open]').onclick=()=>{const r=openCouncilMeeting();if(!r.ok)alert(r.reason||'Unable to open Council meeting.');sig='';render()};return}
 if(!s.topicsComplete){h.innerHTML=`<div class="mysteryHead"><div><small>PEREGRINE COUNCIL</small><h3>Founding testimony</h3></div><strong>TRUST ${s.trust}</strong></div><p class="mysterySummary">Chair Mara Venn will answer the historical record before discussing live route access. The Council distinguishes between the accidental Lacuna Burn, ORISON’s controlled evacuation and the separate reason PEREGRINE later prohibited route activation.</p>${s.canInspect?`<article class="mysteryDirective"><b>VERIFICATION OFFER</b><p>Your first-contact posture left the Council cautious. Accept a passive drive/field inspection and submit the ORISON route package for checksum verification. This grants no command or weapons access.</p><button data-council-inspect>Accept verification · 1 day</button></article>`:''}<div class="mysteryBlock"><b>COUNCIL QUESTIONS</b>${s.topicsList.map(topicCard).join('')}</div>${logHtml(s)}`;h.querySelector('[data-council-inspect]')?.addEventListener('click',()=>{const r=acceptCouncilInspection();if(!r.ok)alert(r.reason||'Inspection failed.');sig='';render()});h.querySelectorAll('[data-council-topic]').forEach(b=>b.onclick=()=>{const r=askCouncilTopic(b.dataset.councilTopic);if(!r.ok)alert(r.reason||'Council discussion failed.');sig='';render()});return}
 if(!s.routePolicy){h.innerHTML=`<div class="mysteryHead"><div><small>PEREGRINE COUNCIL</small><h3>The reciprocal seed</h3></div><strong>TRUST ${s.trust}</strong></div><p class="mysterySummary">The founders preserved a cold return seed rather than a live key. It was deliberately split between three institutions so no captain, engineer or government could reopen the route alone. Your ORISON data makes reconstruction possible again — but not obviously safe.</p>${logHtml(s)}<div class="mysteryBlock"><b>RETURN-ROUTE CUSTODY</b><article class="lead available"><span>Joint reconstruction under independent abort authority</span><p>Accept supervised access to all three custody blocks. Model the route together, then require both fleet and Council approval before any energisation.</p><button data-route-policy="joint">Accept joint custody</button></article><article class="lead available"><span>Defer activation</span><p>Agree that nobody should energise a route yet. Take a non-operational merged archive for modelling while the live custody blocks remain separated.</p><button data-route-policy="defer">Keep the route cold</button></article><article class="lead available"><span>Demand operational geometry</span><p>Argue that a stranded fleet cannot accept indefinite local veto over its route home. Press for the custody blocks now.</p><button data-route-policy="demand">Demand access</button></article></div>`;h.querySelectorAll('[data-route-policy]').forEach(b=>b.onclick=()=>{if(!confirm('Record this return-route custody position?'))return;const r=chooseReturnRoutePolicy(b.dataset.routePolicy);if(!r.ok)alert(r.reason||'Unable to record route policy.');sig='';render()});return}
 const access=s.seedAccess==='full-supervised'?'All three custody blocks are available inside a supervised engineering environment.':s.seedAccess==='archive-only'?'You have a merged non-operational model; the live custody blocks remain physically separated.':'You have only the Navigation custody block and public error model; the other fragments remain protected by PEREGRINE.';
 h.innerHTML=`<div class="mysteryHead"><div><small>PEREGRINE</small><h3>Return geometry</h3></div><strong>${esc(String(s.seedAccess||'RESTRICTED').toUpperCase())}</strong></div><p class="mysterySummary">${esc(access)}</p>${logHtml(s)}<article class="mysteryDirective"><b>WHAT THE FOUNDERS ACTUALLY FEARED</b><p>The evidence does not point to a hidden enemy controlling the network. It points to coupled route physics: repeated or overlapping field solutions can perturb both endpoints, and dangerous precursor harmonics can appear before a route is fully open. PEREGRINE’s taboo is an institutional response to that risk.</p></article><article class="mysteryDirective"><b>NEXT PHASE</b><p>Build the return solution without energising it: reconcile the three custody fragments, model endpoint coupling against the Lacuna Burn, and decide whether an unmanned probe can test the geometry without risking another resonance.</p></article>`
}
render();setInterval(render,500);
