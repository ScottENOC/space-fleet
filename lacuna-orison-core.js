import {campaign,saveCampaign} from './campaign-core.js';

const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function fresh(){return{opened:false,recorderRecovered:false,recorderMethod:null,recorderIntegrity:0,corePath:null,coreOpened:false,findings:[],disclosure:null,complete:false,nextLead:null}}
function ensure(){campaign.mainPlot??={};campaign.mainPlot.orisonCore??=fresh();const s=campaign.mainPlot.orisonCore;s.findings??=[];return s}
function orison(){return campaign.mainPlot?.orison}
function concord(){return campaign.mainPlot?.concord}
function inner(){return campaign.mainPlot?.lacunaInner}
function available(){const o=orison();return campaign.location==='Lacuna Reach'&&!!o?.complete&&!!o?.accessGranted}
function add(text){const s=ensure();s.findings.push({day:campaign.day,text});campaign.log??=[];campaign.log.push(`Day ${campaign.day}: ORISON core — ${text}`)}
function trust(delta){if(!inner())return;inner().trust=clamp((inner().trust||0)+delta,-8,8)}
export function orisonCoreSummary(){const s=ensure(),o=orison();return{...s,available:available(),forcedEntry:!!o?.forcedEntry,sharedCoordinates:o?.disclosure==='share',concordArrangement:concord()?.arrangement||null,canOpen:available()&&!s.opened,canRecover:s.opened&&!s.recorderRecovered,canOpenCore:s.recorderRecovered&&!s.coreOpened,canDisclose:s.coreOpened&&!s.disclosure}}
export function openOrisonInterior(){const s=ensure();if(!available())return{ok:false,reason:'Secure ORISON access first.'};if(s.opened)return{ok:false,reason:'ORISON interior operations are already underway.'};s.opened=true;campaign.day+=1;add(orison()?.forcedEntry?'Boarding teams enter through the damaged outer defence corridor. Several internal buses are isolated and the continuity core remains behind hardened bulkheads.':'ORISON accepts local service access. Boarding teams cross a powered but empty dock toward the closure-era courier and continuity-core trunk.');saveCampaign(campaign);return{ok:true}}
export function recoverCourierRecorder(method){const s=ensure();if(!s.opened)return{ok:false,reason:'Enter ORISON first.'};if(s.recorderRecovered)return{ok:false,reason:'The courier recorder has already been recovered.'};if(!['preserve','rapid'].includes(method))return{ok:false,reason:'Unknown recorder recovery method.'};const cost=method==='preserve'?6:2;if(campaign.supplies<cost)return{ok:false,reason:`Recorder recovery requires ${cost} supplies.`};campaign.supplies-=cost;campaign.day+=1;s.recorderRecovered=true;s.recorderMethod=method;s.recorderIntegrity=method==='preserve'?1:.68;
 if(method==='preserve')add('The courier recorder is removed under external power with its volatile clock and crew log intact. Six courier crew reached ORISON alive, completed the archive hand-off and were transferred into the station rather than attempting a return burn.');
 else add('The recorder is hot-wired out of the cold courier. Some volatile timing data is lost, but the surviving crew log confirms all six crew reached ORISON alive and were moved into the station after the archive upload.');
 saveCampaign(campaign);return{ok:true}}
export function openContinuityCore(method){const s=ensure();if(!s.recorderRecovered)return{ok:false,reason:'Recover the courier recorder before attempting the continuity core.'};if(s.coreOpened)return{ok:false,reason:'The continuity core has already been opened.'};if(!['restore','bypass'].includes(method))return{ok:false,reason:'Unknown core-access method.'};let cost=method==='restore'?8:4;if(orison()?.forcedEntry)cost+=3;if(campaign.supplies<cost)return{ok:false,reason:`This core-access method requires ${cost} supplies.`};campaign.supplies-=cost;campaign.day+=method==='restore'?2:1;s.corePath=method;s.coreOpened=true;
 if(method==='restore'){add('Engineering restores the continuity-core power island without defeating its isolation interlocks. Original event timing, personnel accounting and route-control records survive intact.');if(orison()?.disclosure==='share')trust(1)}
 else{add('Boarding teams bypass the hardened isolation trunk and force a cold data extraction. The method damages several archival buses, but enough route-control state survives to reconstruct the final continuity event.');if(orison()?.disclosure==='share')trust(-1)}
 add('Personnel records show ORISON was not abandoned before the second field event. Twenty-three resident caretakers and the six Lacuna courier crew were processed through the same evacuation muster within forty minutes.');
 add('The second Burn-like signature was deliberate. ORISON invoked Continuity Protocol after the Lacuna archive reported endpoint instability: it energised a single-use outbound route, launched the continuity vessel ARGOSY with twenty-nine people aboard, then destroyed the local return key and entered permanent lock.');
 add('ARGOSY’s destination is identified only as PEREGRINE — another continuity node absent from contemporary charts. The outbound solution is incomplete, but the courier recorder and ORISON core together preserve enough geometry to begin reconstructing it.');
 s.complete=true;s.nextLead='PEREGRINE';campaign.mainPlot.act='peregrine-search';saveCampaign(campaign);return{ok:true,cost}}
export function discloseOrisonFindings(choice){const s=ensure();if(!s.coreOpened)return{ok:false,reason:'Open the continuity core first.'};if(s.disclosure)return{ok:false,reason:'The ORISON findings disclosure is already recorded.'};if(!['share','compartment'].includes(choice))return{ok:false,reason:'Unknown disclosure choice.'};s.disclosure=choice;campaign.day+=1;if(choice==='share'){trust(2);add('The fleet transmits the recovered ORISON personnel ledger, ARGOSY launch record and PEREGRINE route fragment to Concord. Speaker Rhee confirms none of these records survived in the Founding Vault and offers future navigation support.')}else add('The ARGOSY launch record and PEREGRINE route fragment remain compartmented aboard the fleet. Concord is not told that ORISON evacuated a live population beyond Lacuna.');saveCampaign(campaign);return{ok:true}}
if(typeof window!=='undefined')window.__orisonCore={orisonCoreSummary,openOrisonInterior,recoverCourierRecorder,openContinuityCore,discloseOrisonFindings};
