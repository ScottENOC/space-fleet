const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function ensureHost(){const orders=document.querySelector('.orders');if(!orders)return null;let host=document.querySelector('#combatObjectivePanel');if(!host){host=document.createElement('section');host.id='combatObjectivePanel';host.className='combatObjectivePanel';orders.prepend(host)}return host}
function render(){const host=ensureHost();if(!host)return;const s=window.__combatObjectiveSummary?.();if(!s){host.hidden=true;return}host.hidden=false;const t=s.target||{};const state=s.complete?'OBJECTIVE COMPLETE':s.failed?'OBJECTIVE FAILED':'PRIMARY OBJECTIVE';host.innerHTML=`<h2>${esc(state)} // ${esc(s.label)}</h2><p>${esc(s.brief)}</p><div><b>${esc(t.name||'Designated vessel')}</b> · ${Math.round((t.health??1)*100)}% integrity${t.withdrawing?' · WITHDRAWING':''}${t.escaped?' · ESCAPED':''}</div>${s.complete?'<button id="objectiveWithdraw">Objective complete — withdraw fleet</button><p>You may also remain and continue fighting; the contract is already achieved.</p>':''}`;const b=host.querySelector('#objectiveWithdraw');if(b)b.onclick=()=>document.querySelector('#withdrawFleet')?.click()}
render();setInterval(render,300);
