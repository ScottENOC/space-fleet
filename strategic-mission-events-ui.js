import {campaign} from './campaign-core.js';
import {syncStrategicMissionEvents,acceptStrategicMissionEvent} from './strategic-mission-events.js?v=104';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let sig='';
function host(){const base=document.querySelector('#encounterChoices')||document.querySelector('#contractBoard')||document.querySelector('#strategicFrontsPanel');if(!base)return null;let h=document.querySelector('#strategicMissionEventsPanel');if(!h){h=document.createElement('section');h.id='strategicMissionEventsPanel';h.className='investigationPanel mainPlotPanel';base.after(h)}return h}
function buttons(e){if(e.type==='raidResponse')return`<button data-op="${e.id}" data-mode="defend">Respond to distress</button>`;if(e.type==='blockadeBreak')return`<button data-op="${e.id}" data-mode="defend">Challenge blockade</button>`;if(e.type==='logistics')return`<button data-op="${e.id}" data-mode="support">Support convoy</button><button data-op="${e.id}" data-mode="attack">Interdict logistics</button>`;if(e.type==='reinforcement')return`<button data-op="${e.id}" data-mode="attack">Intercept reinforcement</button>`;return''}
function render(){const h=host();if(!h)return;const s=syncStrategicMissionEvents(),local=s.available.filter(e=>e.system===campaign.location),remote=s.available.filter(e=>e.system!==campaign.location);const signature=JSON.stringify([campaign.day,campaign.location,local,remote.length]);if(signature===sig)return;sig=signature;if(!local.length&&!remote.length){h.hidden=true;return}h.hidden=false;h.innerHTML=`<div class="mysteryHead"><div><small>LIVE OPERATIONS</small><h3>Strategic fleet situations</h3></div><strong>${local.length} LOCAL</strong></div><p class="mysterySummary">These opportunities exist because persistent fleets are actually raiding, blockading, escorting logistics or reinforcing. Convoy outcomes change the same fleet's physical supply state as well as readiness, and prolonged pressure now propagates into the local economy. Effects remain recoverable and bounded rather than compounding into faction elimination.</p>${local.map(e=>`<article class="lead available"><span>${esc(e.title)}</span><p>${esc(e.text)}</p><small>${esc(e.factionName)} · ${esc(e.system)} · expires day ${e.expiresDay}</small><div>${buttons(e)}</div></article>`).join('')||'<p>No immediate local operational event.</p>'}${remote.length?`<p><small>${remote.length} other operational situation${remote.length===1?' is':'s are'} active elsewhere.</small></p>`:''}`;h.querySelectorAll('[data-op]').forEach(b=>b.onclick=()=>{const r=acceptStrategicMissionEvent(b.dataset.op,b.dataset.mode);if(!r?.ok)alert(r?.reason||'Unable to act on that operational situation.');sig='';render()})}
render();setInterval(render,900);
