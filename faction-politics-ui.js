import {campaign} from './campaign-core.js';
import {politicsSummary,respondToPoliticalDemand} from './faction-politics.js?v=106';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let sig='';
function host(){const base=document.querySelector('#factionGoalsPanel')||document.querySelector('#strategicEconomyPanel')||document.querySelector('#factionIntel');if(!base)return null;let h=document.querySelector('#factionPoliticsPanel');if(!h){h=document.createElement('section');h.id='factionPoliticsPanel';h.className='investigationPanel mainPlotPanel';base.after(h)}return h}
function alignmentCard(a){return`<article class="lead available"><span>${esc(a.label)}</span><p><b>${esc(a.aName)}</b> + <b>${esc(a.bName)}</b> · ${esc(a.system)}</p><small>Temporary arrangement · review day ${a.endsDay}. It coordinates a current interest without merging the factions or creating permanent alliance obligations.</small></article>`}
function demandCard(d){const rival=d.targetFactionName?` · COUNTERPART ${esc(d.targetFactionName)}`:'';return`<article class="lead available"><span>${esc(d.issuerName)} — ${esc(d.title)}</span><p>This is a political request, not an ordinary paid contract. Accepting it changes how the issuer and any affected rival regard your fleet.</p><small>${esc(d.system)}${rival} · RESPONSE DUE DAY ${d.expiresDay}</small><div><button data-pol="accept" data-id="${esc(d.id)}">Accept</button>${d.targetFaction?`<button data-pol="mediate" data-id="${esc(d.id)}">Mediate</button>`:''}<button data-pol="decline" data-id="${esc(d.id)}">Decline</button></div></article>`}
function render(){const h=host();if(!h)return;const p=politicsSummary(campaign.location),signature=JSON.stringify([campaign.day,campaign.location,p.alignments,p.demands,campaign.factionRep,campaign.supplies]);if(signature===sig)return;sig=signature;if(!p.alignments.length&&!p.demands.length){h.hidden=true;return}h.hidden=false;h.innerHTML=`<div class="mysteryHead"><div><small>FACTION POLITICS</small><h3>Bargaining, alignments & demands</h3></div><strong>${p.demands.length} REQUEST${p.demands.length===1?'':'S'}</strong></div><p class="mysterySummary">Factions now bargain around their persistent strategic goals. Cooperation is temporary and issue-specific; success can create leverage, failure can trigger reassessment, and even friendly factions may disagree over the next priority.</p>${p.alignments.length?`<h4>Temporary alignments</h4>${p.alignments.map(alignmentCard).join('')}`:''}${p.demands.length?`<h4>Requests to your fleet</h4>${p.demands.map(demandCard).join('')}`:''}`;h.querySelectorAll('[data-pol]').forEach(b=>b.onclick=()=>{const action=b.dataset.pol,id=b.dataset.id;if(action==='accept'&&!confirm('Accept this political commitment? It may cost supplies, a campaign day and standing with another faction.'))return;const r=respondToPoliticalDemand(id,action);if(!r?.ok)alert(r?.reason||'The request is no longer active.');sig='';render()})}
render();setInterval(render,1050);