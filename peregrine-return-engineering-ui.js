import {campaign} from './campaign-core.js';
import {returnEngineeringSummary,runReturnModel,launchReturnProbe} from './peregrine-return-engineering.js?v=92';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let sig='';
function host(){const base=document.querySelector('#peregrineCouncilPanel')||document.querySelector('#peregrineContactPanel');if(!base)return null;let h=document.querySelector('#peregrineReturnEngineeringPanel');if(!h){h=document.createElement('section');h.id='peregrineReturnEngineeringPanel';h.className='investigationPanel mainPlotPanel';base.after(h)}return h}
function logHtml(s){return(s.history||[]).length?`<div class="mysteryBlock"><b>ENGINEERING LOG</b><ul>${s.history.map(x=>`<li>Day ${x.day}: ${esc(x.text)}</li>`).join('')}</ul></div>`:''}
function modelCard(m){return `<article class="lead ${m.available?'available':''}"><span>${esc(m.name)}</span><p>${esc(m.summary)}</p><small>${m.resolved?'MODEL COMPLETE':`${m.cost} supplies · ${m.days} days`}</small>${m.available?`<button data-return-model="${m.id}">Run model</button>`:''}</article>`}
function accessLabel(a){return a==='full-supervised'?'FULL SUPERVISED SEED':a==='archive-only'?'ARCHIVE-ONLY SEED':'PARTIAL SEED'}
function render(){const h=host();if(!h)return;const s=returnEngineeringSummary();if(!s.available){h.hidden=true;return}h.hidden=false;const signature=JSON.stringify([campaign.day,campaign.supplies,s]);if(signature===sig)return;sig=signature;
 if(!s.modelComplete){h.innerHTML=`<div class="mysteryHead"><div><small>PEREGRINE</small><h3>Return-route engineering</h3></div><strong>${accessLabel(s.seedAccess)}</strong></div><p class="mysterySummary">Do not energise a route yet. First reproduce the Burn, the ARGOSY wake and the abort envelope entirely in simulation. The objective is to reduce a system-wide risk problem to something small enough for an unmanned probe.</p><div class="mysteryBlock"><b>NON-ENERGISING MODELS</b>${s.modelOps.map(modelCard).join('')}</div>${logHtml(s)}`;h.querySelectorAll('[data-return-model]').forEach(b=>b.onclick=()=>{const r=runReturnModel(b.dataset.returnModel);if(!r.ok)alert(r.reason||'Engineering model failed.');sig='';render()});return}
 if(!s.probeMode){h.innerHTML=`<div class="mysteryHead"><div><small>PEREGRINE</small><h3>Unmanned reciprocal probe</h3></div><strong>${Math.round(s.confidence*100)}% MODEL CONFIDENCE</strong></div><p class="mysterySummary">The modelling says a return solution may exist, but a crewed transit would still be reckless. Test phase lock and endpoint response with a disposable package that cannot carry people.</p>${logHtml(s)}<div class="mysteryBlock"><article class="lead available"><span>Conservative probe</span><p>Use a fully instrumented package and abort well below the historical precursor threshold. More expensive, but designed to fail safely.</p><small>10 supplies · 2 days</small><button data-return-probe="conservative">Launch conservative probe</button></article><article class="lead available"><span>Boundary probe</span><p>Push closer to the Council’s outer abort limit to get a stronger endpoint reflection. Cheaper and faster, but creates a larger field wake and may cost trust.</p><small>5 supplies · 1 day</small><button data-return-probe="boundary">Launch boundary probe</button></article></div>`;h.querySelectorAll('[data-return-probe]').forEach(b=>b.onclick=()=>{if(!confirm('Launch this unmanned route probe?'))return;const r=launchReturnProbe(b.dataset.returnProbe);if(!r.ok)alert(r.reason||'Probe launch failed.');sig='';render()});return}
 h.innerHTML=`<div class="mysteryHead"><div><small>PEREGRINE</small><h3>${s.probeSuccess?'Reciprocal lock demonstrated':'Return solution still incomplete'}</h3></div><strong>${s.probeSuccess?'PROBE SUCCESS':'SAFE ABORT'}</strong></div><p class="mysterySummary">${esc(s.probeResult||'Probe test complete.')}</p>${logHtml(s)}<article class="mysteryDirective"><b>NEXT PHASE</b><p>${s.probeSuccess?'A return route is now physically plausible, but the Council still has to decide whether a crewed opening is worth the residual risk — and you must decide whether to accept their conditions, wait for a safer window, or attempt a more unilateral solution.':'The engineering problem has narrowed to missing custody information rather than unknown physics. Recovering or renegotiating access to the remaining seed blocks is now necessary before another probe can be justified.'}</p><small>Current PEREGRINE trust: ${s.trust}</small></article>`
}
render();setInterval(render,500);
